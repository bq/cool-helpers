before(function() {
    Handlebars = require("handlebars"),
    coolHelpers = require("../../lib/cool-helpers"),
    chai = require('chai'),
    helpers = Handlebars.helpers,
    expect = chai.expect;
});
